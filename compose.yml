services:
  app:
    build: .
    environment:
      - PUID=1000
      - PGID=100
      - TZ=America/Los_Angeles
      - CRON_SCHEDULE=* * * * *
      # - COMMAND=echo "Hello"
      - SCRIPTS=test.sh,test_two.sh
    volumes:
      - ./scripts:/app/scripts
